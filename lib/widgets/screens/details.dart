import 'package:flutter/material.dart';

class Detals extends StatelessWidget {
  const Detals({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Center(
        child: Text("TUGAD.. (:", style: TextStyle(
          fontSize: 50
        ),),
      ),
    );
  }
}
